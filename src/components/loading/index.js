import * as S from './style';

const Loading = () => {
  return (
    <S.LoadingWrapper>
      <img src="images/icon_splash.png" alt="back" />
    </S.LoadingWrapper>
  );
};

export default Loading;
